// Used in the utility section of the runtime

struct TonemapSettings {
    scale:           f32,
    use_gamma:       bool,
    method:          i32,
    exposure_factor: f32,
    exposure_offset: f32
}
